export enum DiscountAmountType { Flat = 1, PerOrderAmount }
export enum DiscountRuleType { NoRule = 1, MinOrderValue }
export enum OrderStatus { Pending = 1, Confirmed, Paid, Picked, Onway, Delivered, Cancelled }
